# 📝 编辑器搜索完整指南

## 🎯 搜索方式对比

| 搜索方式 | 优势 | 劣势 | 适用场景 |
|----------|------|------|----------|
| **编辑器内置搜索** | 速度快、集成度高、实时预览 | 功能相对简单 | 日常快速搜索 |
| **我们的搜索引擎** | 功能强大、支持模糊搜索、跨文件类型 | 需要额外工具 | 复杂搜索需求 |
| **混合使用** | 结合两者优势 | 需要学习成本 | 专业开发工作 |

---

## 🔍 VSCode 内置搜索详解

### **基本搜索快捷键**
```
Ctrl+F          # 当前文件搜索
Ctrl+Shift+F    # 全局搜索
Ctrl+H          # 当前文件替换
Ctrl+Shift+H    # 全局替换
Ctrl+G          # 跳转到行
Ctrl+P          # 快速打开文件
Ctrl+Shift+O    # 当前文件符号搜索
Ctrl+T          # 全局符号搜索
```

### **高级搜索选项**
- **正则表达式**: 点击 `.*` 按钮或按 `Alt+R`
- **大小写敏感**: 点击 `Aa` 按钮或按 `Alt+C`  
- **全词匹配**: 点击 `Ab` 按钮或按 `Alt+W`
- **文件包含/排除**: 在搜索框下方设置过滤条件

### **搜索结果操作**
- **预览**: 点击结果直接预览
- **跳转**: 双击结果跳转到文件
- **替换**: 在搜索结果中直接编辑
- **折叠**: 按文件分组显示结果

---

## 🚀 集成我们的搜索引擎

### **设置编辑器集成**
```bash
# 一键设置所有编辑器集成
python editor_search_integration.py --setup
```

### **VSCode 集成使用**

#### **方法1: 任务面板**
1. 按 `Ctrl+Shift+P`
2. 输入 "Tasks: Run Task"
3. 选择 "编辑器搜索: 交互式搜索"
4. 输入搜索关键词

#### **方法2: 选中文本搜索**
1. 选中要搜索的文本
2. 按 `Ctrl+Shift+P`
3. 选择 "编辑器搜索: 当前选中文本"

#### **方法3: 自定义快捷键**
在 `keybindings.json` 中添加：
```json
[
    {
        "key": "ctrl+alt+f",
        "command": "workbench.action.tasks.runTask",
        "args": "编辑器搜索: 交互式搜索"
    }
]
```

---

## 🎨 Sublime Text 集成

### **内置搜索**
```
Ctrl+F          # 当前文件搜索
Ctrl+Shift+F    # 全局搜索
Ctrl+G          # 跳转到行
Ctrl+P          # 快速打开文件
Ctrl+R          # 当前文件符号搜索
Ctrl+Shift+R    # 全局符号搜索
```

### **集成我们的搜索**
设置完成后可以使用：
- `Ctrl+Shift+P` → "高级搜索: 关键词搜索"
- `Ctrl+Alt+F` → 搜索选中文本

---

## ⚡ Vim/NeoVim 集成

### **内置搜索**
```vim
/pattern        " 向下搜索
?pattern        " 向上搜索
n               " 下一个匹配
N               " 上一个匹配
:line_number    " 跳转到行
:vimgrep /pattern/ **/*  " 全局搜索
```

### **集成我们的搜索**
添加到 `~/.vimrc`：
```vim
" 搜索当前词
nnoremap <leader>fs :!python workspace_jumper.py <cword><CR>

" 搜索选中文本  
vnoremap <leader>fs y:!python workspace_jumper.py <C-R>"<CR>

" 交互式搜索
nnoremap <leader>fi :!python workspace_jumper.py 
```

---

## 🔄 最佳实践：混合使用策略

### **日常快速搜索 → 编辑器内置**
```
场景: 在当前文件中查找变量名
方法: Ctrl+F 输入变量名
优势: 即时高亮，快速跳转
```

### **跨文件搜索 → 编辑器全局搜索**
```
场景: 查找函数在哪些文件中被调用
方法: Ctrl+Shift+F 输入函数名
优势: 实时预览，可直接编辑
```

### **复杂模式搜索 → 我们的搜索引擎**
```
场景: 模糊搜索相似概念，或复杂正则表达式
方法: python workspace_jumper.py "模糊词" 
优势: 智能匹配，丰富的搜索选项
```

### **学习和研究 → 混合使用**
```
1. 用编辑器快速定位大概位置
2. 用我们的搜索引擎深入查找相关内容
3. 用编辑器的符号搜索理解代码结构
```

---

## 📊 功能对比表

| 功能 | VSCode内置 | 我们的搜索引擎 | 推荐使用 |
|------|------------|----------------|----------|
| **速度** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | VSCode内置 |
| **模糊搜索** | ⭐⭐ | ⭐⭐⭐⭐⭐ | 我们的引擎 |
| **正则表达式** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 我们的引擎 |
| **文件类型过滤** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 两者都好 |
| **结果预览** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | VSCode内置 |
| **批量替换** | ⭐⭐⭐⭐⭐ | ⭐ | VSCode内置 |
| **上下文显示** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 我们的引擎 |
| **书签管理** | ⭐⭐ | ⭐⭐⭐⭐⭐ | 我们的引擎 |
| **跨编辑器** | ⭐ | ⭐⭐⭐⭐⭐ | 我们的引擎 |

---

## 🎯 使用建议

### **新手推荐**
1. **先掌握编辑器内置搜索**
   - 学会 `Ctrl+F` 和 `Ctrl+Shift+F`
   - 熟悉基本的搜索选项

2. **逐步引入我们的搜索引擎**
   - 从简单的关键词搜索开始
   - 体验交互式跳转功能

### **进阶用户**
1. **设置编辑器集成**
   - 配置快捷键和任务
   - 建立个人搜索工作流

2. **根据场景选择工具**
   - 快速查找 → 编辑器内置
   - 深度研究 → 我们的引擎
   - 学习新概念 → 混合使用

### **专业开发者**
1. **自定义搜索命令**
   - 创建项目特定的搜索任务
   - 集成到CI/CD流程中

2. **建立搜索知识库**
   - 使用书签功能保存重要位置
   - 创建常用搜索的快捷方式

---

## 🚀 快速上手

### **5分钟体验**
```bash
# 1. 设置集成
python editor_search_integration.py --setup

# 2. 在VSCode中试试
# 按 Ctrl+Shift+P → "Tasks: Run Task" → "编辑器搜索: 交互式搜索"

# 3. 搜索一个关键词，体验跳转功能
```

### **日常使用流程**
```
1. 编辑器内快速搜索 (Ctrl+F, Ctrl+Shift+F)
   ↓ 如果需要更复杂的搜索
2. 使用我们的搜索引擎 (workspace_jumper.py)
   ↓ 找到目标后
3. 直接跳转到编辑器继续工作
```

---

## 💡 总结

**编辑器内置搜索** 和 **我们的搜索引擎** 各有优势，最佳策略是：

- 🚀 **日常快速搜索** → 编辑器内置
- 🔍 **复杂深度搜索** → 我们的搜索引擎  
- 🎯 **学习研究场景** → 两者结合使用

通过合理的工具选择和工作流设计，您可以获得最高效的搜索体验！
