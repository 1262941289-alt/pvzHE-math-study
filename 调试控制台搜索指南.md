# 🔧 调试控制台搜索完整指南

## 🎯 概述

调试控制台搜索是一个专门为开发者设计的交互式搜索工具，让您可以在Python调试控制台中直接使用我们强大的搜索引擎，并支持文件跳转功能。

---

## 🚀 快速开始

### **方法1: 直接启动调试控制台**

```bash
python -i debug_console_search.py
```

### **方法2: 在VSCode中使用任务**

1. 按 `Ctrl+Shift+P`
2. 输入 "Tasks: Run Task"
3. 选择 "调试控制台搜索"

### **方法3: 导入到现有Python会话**

```python
from debug_console_search import *
```

---

## 📋 基本命令

### **搜索命令**

| 命令 | 完整形式 | 功能 | 示例 |
|------|----------|------|------|
| `s()` | `search()` | 关键词搜索 | `s("原函数")` |
| `r()` | `regex_search()` | 正则表达式搜索 | `r("def\\s+\\w+")` |
| `f()` | `fuzzy_search()` | 模糊搜索 | `f("积分", 0.8)` |

### **结果操作命令**

| 命令 | 完整形式 | 功能 | 示例 |
|------|----------|------|------|
| `j()` | `jump()` | 跳转到结果 | `j(1)` |
| `show()` | `show()` | 显示上下文 | `show(2, 10)` |
| `h()` | `help_search()` | 显示帮助 | `h()` |

---

## 🔍 搜索示例

### **1. 基本关键词搜索**

```python
>>> s("原函数")
🔍 搜索: 原函数
🎯 找到 84 个结果:
[ 1] demo_editor_search.py:186
     print("5. 输入 '原函数' 开始搜索")
[ 2] README_搜索引擎.md:51
     python search_engine.py -m "原函数" "积分" -o AND
...

💡 使用 jump(数字) 跳转到结果
💡 使用 show(数字) 显示更多上下文
```

### **2. 正则表达式搜索**

```python
>>> r("def\\s+\\w+")
🔍 正则搜索: def\s+\w+
🎯 找到 125 个结果:
[ 1] debug_console_search.py:18
     def __init__(self, workspace_path: str = "."):
[ 2] debug_console_search.py:23
     def search(self, keyword: str, **kwargs) -> List[SearchResult]:
...
```

### **3. 模糊搜索**

```python
>>> f("积分", 0.7)
🔍 模糊搜索: 积分 (相似度 >= 0.7)
🎯 找到 45 个结果:
...
```

### **4. 带参数的搜索**

```python
>>> s("函数", extensions=['md'], context_lines=3)
>>> r("class\\s+\\w+", extensions=['py'])
>>> f("导数", 0.8, extensions=['md', 'txt'])
```

---

## 🎯 结果操作

### **跳转到文件**

```python
>>> j(1)        # 跳转到第1个结果
✅ 已在VSCode中打开 demo_editor_search.py:186

>>> jump(5)     # 完整命令形式
```

### **显示上下文**

```python
>>> show(1)     # 显示第1个结果的5行上下文
>>> show(2, 10) # 显示第2个结果的10行上下文

📁 数学\第08讲-不定积分.md:53
==================================================
    48:
    49: ---
    50:
    51: ## 📋 知识点结构
    52:
➤   53: ### 1️⃣ 原函数与不定积分的概念
    54:
    55: ---
    56:
    57: #### 1.1 原函数的定义
    58:
```

---

## ⚙️ 高级功能

### **搜索选项参数**

```python
# 大小写敏感搜索
s("Function", case_sensitive=True)

# 只搜索特定文件类型
s("原函数", extensions=['md', 'txt'])

# 显示更多上下文
s("积分", context_lines=5)

# 排除特定目录
s("函数", exclude_dirs=['node_modules', '.git'])
```

### **组合使用**

```python
# 1. 搜索函数定义
>>> r("def\\s+\\w+")

# 2. 查看第3个结果的详细上下文
>>> show(3, 8)

# 3. 跳转到该函数
>>> j(3)

# 4. 搜索相关概念
>>> f("函数定义", 0.8)
```

---

## 🔧 VSCode集成

### **设置VSCode任务**

调试控制台搜索已经集成到VSCode任务中：

1. **启动调试控制台**:
   - `Ctrl+Shift+P` → "Tasks: Run Task" → "调试控制台搜索"

2. **在任务中使用**:

   ```json
   {
     "label": "调试控制台搜索",
     "type": "shell",
     "command": "python",
     "args": ["-i", "debug_console_search.py"]
   }
   ```

### **与其他工具结合**

```python
# 在调试控制台中导入其他模块
>>> from workspace_jumper import WorkspaceJumper
>>> jumper = WorkspaceJumper()

# 结合使用
>>> s("原函数")
>>> j(1)  # 跳转到VSCode
```

---

## 💡 使用技巧

### **1. 快速搜索工作流**

```python
# 步骤1: 快速搜索
>>> s("积分")

# 步骤2: 查看感兴趣的结果
>>> show(5)

# 步骤3: 跳转编辑
>>> j(5)

# 步骤4: 搜索相关内容
>>> f("不定积分", 0.8)
```

### **2. 调试代码时的搜索**

```python
# 搜索函数定义
>>> r("def\\s+search_\\w+")

# 搜索类定义
>>> r("class\\s+\\w+Search")

# 搜索特定错误
>>> s("Exception", extensions=['py'])
```

### **3. 学习新概念**

```python
# 模糊搜索相关概念
>>> f("原函数", 0.7)

# 查看定义和解释
>>> show(1, 10)

# 跳转到详细文档
>>> j(1)
```

### **4. 代码审查**

```python
# 搜索TODO和FIXME
>>> s("TODO")
>>> s("FIXME")

# 搜索特定模式
>>> r("# TODO:.*")
```

---

## 🎨 自定义配置

### **修改搜索引擎设置**

```python
# 访问搜索工具实例
>>> search_tool.search_engine.root_path
'.'

# 修改默认设置
>>> search_tool.search_engine.default_extensions = ['py', 'md', 'txt']
```

### **创建自定义搜索函数**

```python
def search_functions():
    """搜索所有函数定义"""
    return r("def\\s+\\w+", extensions=['py'])

def search_classes():
    """搜索所有类定义"""
    return r("class\\s+\\w+", extensions=['py'])

# 使用自定义函数
>>> search_functions()
>>> search_classes()
```

---

## 🐛 故障排除

### **常见问题**

#### **1. 跳转失败**

```python
>>> j(1)
❌ 无法打开VSCode: 命令未找到
```

**解决方案**: 
- 确保VSCode已安装并在PATH中
- 尝试使用 `show()` 命令查看文件内容

#### **2. 搜索结果为空**

```python
>>> s("不存在的内容")
❌ 未找到匹配结果
```

**解决方案**:
- 检查关键词拼写
- 尝试模糊搜索: `f("关键词", 0.6)`
- 检查文件扩展名过滤

#### **3. 正则表达式错误**

```python
>>> r("[invalid")
❌ 正则表达式语法错误
```

**解决方案**:
- 检查正则表达式语法
- 使用双反斜杠转义: `r("def\\s+\\w+")`

---

## 📊 性能优化

### **搜索优化技巧**

```python
# 限制搜索范围
s("关键词", extensions=['md'])  # 只搜索Markdown文件

# 排除大文件夹
s("关键词", exclude_dirs=['node_modules', 'build', 'dist'])

# 限制结果数量
s("关键词", max_results=10)
```

### **内存使用优化**

```python
# 清理搜索历史
search_tool.last_results = []

# 重新创建搜索引擎实例
search_tool = DebugConsoleSearch()
```

---

## 🎉 总结

调试控制台搜索工具提供了：

✅ **简洁的命令**: `s()`, `r()`, `f()`, `j()`, `show()`
✅ **强大的搜索**: 关键词、正则、模糊搜索
✅ **便捷的跳转**: 直接跳转到VSCode指定行
✅ **丰富的上下文**: 显示匹配行的周围内容
✅ **VSCode集成**: 无缝集成到开发环境
✅ **高度可定制**: 支持各种搜索参数

### **立即开始使用**

```bash
# 启动调试控制台
python -i debug_console_search.py

# 开始搜索
>>> s("原函数")
>>> j(1)
```

这个工具将大大提高您的代码搜索和导航效率！🚀
