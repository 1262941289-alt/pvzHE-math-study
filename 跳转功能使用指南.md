# 🚀 搜索引擎跳转功能使用指南

## 📋 功能概述

搜索引擎现在支持多种跳转功能和工作区集成，让您可以直接从搜索结果跳转到文件，大大提高工作效率。

---

## 🔧 功能特性

### ✨ 文件跳转功能
- **VSCode跳转**: 直接在VSCode中打开文件并跳转到指定行
- **记事本打开**: 用系统记事本打开文件
- **文件夹打开**: 打开文件所在的文件夹
- **默认编辑器**: 用系统默认编辑器打开文件

### 🎯 工作区集成
- **VSCode任务集成**: 在VSCode中创建搜索任务
- **代码片段**: 快速输入搜索命令的代码片段
- **快捷键支持**: 自定义快捷键快速搜索
- **输入提示**: 智能输入提示和自动完成

---

## 🚀 使用方法

### 1️⃣ 交互式命令行搜索

```bash
# 启动交互式搜索
python search_engine.py -k "原函数" --interactive

# 带上下文的交互式搜索
python search_engine.py -k "积分" -c 2 --interactive
```

**操作说明：**
- 输入数字 + 回车：在VSCode中打开对应文件
- 输入数字 + 'n'：在记事本中打开
- 输入数字 + 'f'：打开文件所在文件夹
- 输入 'q'：退出

### 2️⃣ Web界面跳转

启动Web服务：
```bash
python search_web.py
```

访问：http://localhost:5000

**Web界面功能：**
- 🔍 实时搜索结果显示
- 📝 VSCode打开按钮
- 📁 打开文件夹按钮
- 📄 记事本打开按钮
- 🔔 操作结果通知

### 3️⃣ VSCode工作区集成

#### 设置集成
```bash
python vscode_integration.py --setup
```

#### 使用方法
1. **任务面板**：
   - 按 `Ctrl+Shift+P`
   - 输入 "Tasks: Run Task"
   - 选择搜索任务（如"搜索: 函数"）

2. **代码片段**：
   - 输入 `search` 然后按 `Tab`
   - 输入 `regex-search` 然后按 `Tab`
   - 输入 `multi-search` 然后按 `Tab`

3. **自定义搜索**：
   - 选择"搜索: 自定义"任务
   - 输入您要搜索的关键词

### 4️⃣ 快速启动工具

```bash
# 交互式菜单
python quick_search.py

# 直接搜索
python quick_search.py "函数定义"

# 设置工作区
python quick_search.py --setup
```

---

## 📁 文件结构

```
搜索引擎文件/
├── search_engine.py          # 核心搜索引擎（增强版）
├── search_web.py             # Web界面（支持跳转）
├── vscode_integration.py     # VSCode集成工具
├── quick_search.py           # 快速启动工具
├── test_search.py            # 测试脚本
├── README_搜索引擎.md        # 详细说明文档
├── 跳转功能使用指南.md       # 本文档
└── .vscode/                  # VSCode配置
    ├── tasks.json            # 搜索任务配置
    └── search.code-snippets  # 代码片段
```

---

## 🎯 VSCode集成详解

### 任务配置
自动创建的搜索任务：
- 搜索: 函数
- 搜索: 定义
- 搜索: TODO
- 搜索: FIXME
- 搜索: BUG
- 搜索: 自定义

### 代码片段
- `search`: 基本搜索命令
- `regex-search`: 正则表达式搜索
- `multi-search`: 多关键词搜索

### 快捷键（可自定义）
- `Ctrl+Shift+F`: 快速搜索
- `Ctrl+Shift+R`: 正则搜索

---

## 🌐 Web界面功能

### 搜索选项
- **搜索模式**: 关键词、正则表达式、模糊搜索
- **文件过滤**: 按扩展名过滤
- **上下文**: 显示匹配行的上下文
- **选项**: 忽略大小写、全词匹配

### 跳转按钮
每个搜索结果都有三个操作按钮：
- **📝 VSCode打开**: 在VSCode中打开文件并跳转到指定行
- **📁 打开文件夹**: 打开文件所在的文件夹
- **📄 记事本打开**: 用记事本打开文件

### 通知系统
- ✅ 成功操作的绿色通知
- ❌ 失败操作的红色通知
- 自动消失的通知提示

---

## 🔧 高级配置

### 自定义VSCode命令
如果您使用的不是标准的VSCode，可以修改 `search_engine.py` 中的 `vscode_commands` 列表：

```python
vscode_commands = ['code', 'cursor', 'codium', 'your-editor']
```

### 自定义搜索关键词
设置工作区时可以添加自定义关键词：

```bash
python vscode_integration.py --setup --keywords 函数 定义 TODO 自定义词1 自定义词2
```

### Web服务配置
修改 `search_web.py` 中的配置：
- 端口号：默认5000
- 访问模式：本机访问或局域网访问
- 调试模式：开发时启用

---

## 🐛 故障排除

### VSCode无法打开
**问题**: 点击VSCode按钮没有反应
**解决**: 
1. 确保VSCode已安装并在PATH中
2. 尝试在命令行运行 `code --version`
3. 如果使用Cursor等其他编辑器，会自动尝试

### Web界面跳转失败
**问题**: Web界面的跳转按钮不工作
**解决**:
1. 检查文件路径是否正确
2. 确保有相应软件的访问权限
3. 查看浏览器控制台的错误信息

### 任务无法运行
**问题**: VSCode任务面板中的搜索任务无法运行
**解决**:
1. 确保Python在PATH中
2. 检查 `.vscode/tasks.json` 文件是否正确
3. 重新运行 `python vscode_integration.py --setup`

---

## 💡 使用技巧

### 1. 高效搜索
- 使用引号搜索精确短语：`"原函数定义"`
- 使用正则表达式搜索模式：`def\s+\w+`
- 组合多个关键词：`python search_engine.py -m "函数" "定义" -o AND`

### 2. 快速跳转
- 在交互模式中，直接输入数字快速跳转
- 使用Web界面的按钮进行可视化操作
- 利用VSCode任务快速启动搜索

### 3. 工作流优化
- 设置常用搜索关键词的VSCode任务
- 使用代码片段快速输入搜索命令
- 结合文件夹跳转功能快速定位文件位置

---

## 🎉 总结

通过这些跳转功能和工作区集成，搜索引擎现在不仅能找到内容，还能直接带您到达目标位置，真正实现了"搜索即跳转"的高效工作流程！

**主要优势：**
- 🚀 **效率提升**: 从搜索到编辑一步到位
- 🎯 **精确定位**: 直接跳转到指定行号
- 🔧 **深度集成**: 与VSCode无缝集成
- 🌐 **多种方式**: 命令行、Web界面、编辑器任务
- 📱 **用户友好**: 直观的操作界面和提示

开始使用这些强大的功能，让您的文件搜索和编辑工作更加高效！🎯
